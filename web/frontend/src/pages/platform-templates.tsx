import { ArrowLeft, Check, Plus, X } from 'lucide-react';
import { FormEvent, useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { Button } from '@/components/ui/button';
import { Card } from '@/components/ui/card';
import { ErrorState, LoadingState } from '@/components/states';
import { api, patch, post } from '@/lib/api';
import { PlatformTemplate } from '@/types';

type Editing = Partial<PlatformTemplate>;

export function PlatformTemplatesPage() {
  const [items, setItems] = useState<PlatformTemplate[]>(); const [editing, setEditing] = useState<Editing>(); const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  const load = () => api<PlatformTemplate[]>('/admin/templates').then(setItems).catch((reason) => setError(reason.message));
  useEffect(() => { void load(); }, []);
  const save = async (event: FormEvent) => { event.preventDefault(); if (!editing?.title?.trim() || !editing.content?.trim()) return setError('标题和内容不能为空'); setBusy(true); try { const body = { title: editing.title, content: editing.content, category: editing.category || null, isActive: editing.isActive ?? true }; editing.id ? await patch(`/admin/templates/${editing.id}`, body) : await post('/admin/templates', body); setEditing(undefined); await load(); } catch (reason) { setError((reason as Error).message); } finally { setBusy(false); } };
  const toggle = async (item: PlatformTemplate) => { try { await patch(`/admin/templates/${item.id}`, { isActive: !item.isActive }); await load(); } catch (reason) { setError((reason as Error).message); } };
  if (!items) return error ? <ErrorState message={error} retry={load} /> : <LoadingState />;
  return <div className="page-enter space-y-6"><header className="flex flex-wrap items-end gap-3"><Button asChild variant="ghost" size="icon"><Link to="/admin"><ArrowLeft className="h-4 w-4" /></Link></Button><div className="mr-auto"><p className="font-mono text-[11px] uppercase tracking-[.22em] text-neutral-400">Content Library</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.03em]">行业话术库</h1><p className="mt-2 text-sm text-neutral-500">只有启用并审核后的平台模板会对普通用户可见。</p></div><Button onClick={() => setEditing({ title: '', content: '', category: '', isActive: true })}><Plus className="h-4 w-4" />新增模板</Button></header>{error && <ErrorState message={error} retry={load} />}<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map((item) => <Card key={item.id} className="flex flex-col p-5"><div className="flex items-start justify-between gap-3"><div><span className="rounded bg-neutral-100 px-2 py-1 text-[10px] text-neutral-600">{item.category || '未分类'}</span><h2 className="mt-3 font-semibold">{item.title}</h2></div>{item.isActive && <Check className="h-4 w-4 shrink-0" />}</div><p className="mt-4 line-clamp-5 whitespace-pre-wrap text-sm leading-6 text-neutral-600">{item.content}</p><div className="mt-auto flex gap-2 pt-5"><Button className="flex-1" variant="outline" onClick={() => setEditing(item)}>编辑</Button><Button variant={item.isActive ? 'ghost' : 'default'} onClick={() => void toggle(item)}>{item.isActive ? '停用' : '发布'}</Button></div></Card>)}</div>{editing && <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/35 p-4 sm:items-center"><Card className="w-full max-w-2xl p-6 shadow-2xl"><div className="flex items-center justify-between"><h2 className="text-xl font-semibold">{editing.id ? '编辑平台模板' : '新增平台模板'}</h2><Button variant="ghost" size="icon" onClick={() => setEditing(undefined)}><X className="h-4 w-4" /></Button></div><form className="mt-6 space-y-4" onSubmit={save}><div className="grid gap-3 sm:grid-cols-2"><label><span className="field-label">标题</span><input className="h-10 w-full rounded-md border border-neutral-300 px-3 text-sm" value={editing.title ?? ''} onChange={(event) => setEditing({ ...editing, title: event.target.value })} /></label><label><span className="field-label">行业分类</span><input className="h-10 w-full rounded-md border border-neutral-300 px-3 text-sm" value={editing.category ?? ''} onChange={(event) => setEditing({ ...editing, category: event.target.value })} placeholder="例如：零售门店" /></label></div><label><span className="field-label">模板正文</span><textarea className="textarea min-h-56" value={editing.content ?? ''} onChange={(event) => setEditing({ ...editing, content: event.target.value })} placeholder="可使用 {{friend_name}}、{{date}} 等变量" /></label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={editing.isActive ?? true} onChange={(event) => setEditing({ ...editing, isActive: event.target.checked })} />保存后对普通用户可见</label><Button className="w-full" disabled={busy}>{busy ? '保存中…' : '保存模板'}</Button></form></Card></div>}</div>;
}
